apiVersion: apps/v1
kind: Deployment
metadata:
  name: alergiscan
  namespace: alergiscan
  labels:
    app: alergiscan
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alergiscan
  template:
    metadata:
      labels:
        app: alergiscan
    spec:
      containers:
        - name: alergiscan
          image: ghcr.io/ilmimris/alergiscan:latest
          ports:
            - containerPort: 3000
              name: http
              protocol: TCP
          envFrom:
            - secretRef:
                name: alergiscan-secrets
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
